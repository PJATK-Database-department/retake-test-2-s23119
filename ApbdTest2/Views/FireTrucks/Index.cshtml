@model IEnumerable<ApbdTest2.Models.FireTruck>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OperationalNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SpecialEquipment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OperationalNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SpecialEquipment)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdFireTruck">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdFireTruck">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdFireTruck">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
